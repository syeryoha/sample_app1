<% if @user != current_user %>
  <div id="follow_form">
  <% if !current_user.following?(@user) %>
	<%= render 'follow' %>
  <% else %>
	<%= render 'unfollow' %>
  <% end %>
  </div>
 <% end %>